# The app uses a local Ollama backend (OpenAI-compatible API); no API key required.
# Override the defaults only if your Ollama server differs.
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_MODEL=llama3.1
